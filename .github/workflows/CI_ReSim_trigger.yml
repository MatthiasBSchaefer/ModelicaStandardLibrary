name: Trigger CI ReSim

on:
  pull_request:
  workflow_dispatch:
  
env:
  EVENT_NUMBER:  ${{ github.event.number }}
  PR_SHA: ${{ github.event.pull_request.base.sha }}
  
jobs:
  get_PR_data:
    runs-on: ubuntu-latest
    steps:
      - name: print vars
        run: |
            echo "Event Number: ${{ env.EVENT_NUMBER  }}"
            echo "PR Base Hash: ${{ env.PR_SHA  }}"             